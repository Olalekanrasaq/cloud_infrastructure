version: 3
projects:
- name: aws-project
  dir: aws
  autoplan:
    when_modified:
      - "*.tf"
    enabled: true

- name: snowflake-project
  dir: snowflake
  autoplan:
    when_modified:
      - "*.tf"
    enabled: true